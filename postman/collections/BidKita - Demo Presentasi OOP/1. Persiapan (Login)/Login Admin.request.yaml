$kind: http-request
url: "{{base_url}}/api/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "admin@bidkita.com",
      "password": "admin123"
    }
scripts:
  - type: afterResponse
    code: pm.collectionVariables.set('admin_token', pm.response.json().accessToken);
    language: text/javascript
order: 1000
