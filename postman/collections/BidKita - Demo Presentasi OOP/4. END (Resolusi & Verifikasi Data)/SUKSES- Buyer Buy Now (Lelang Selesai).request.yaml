$kind: http-request
name: "SUKSES: Buyer Buy Now (Lelang Selesai)"
url: "{{base_url}}/api/auctions/{{demo_auction_id}}/buynow"
method: POST
headers:
  Content-Type: application/json
auth:
  type: bearer
  credentials:
    token: "{{buyer_token}}"
order: 2000
