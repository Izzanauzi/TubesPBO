$kind: http-request
name: "4. GAGAL: Bid Terlalu Rendah (Encapsulation)"
url: "{{base_url}}/api/auctions/{{auction_elek_id}}/bid"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "amount": 1000000
    }
auth:
  type: bearer
  credentials:
    token: "{{buyer_token}}"
order: 5000
