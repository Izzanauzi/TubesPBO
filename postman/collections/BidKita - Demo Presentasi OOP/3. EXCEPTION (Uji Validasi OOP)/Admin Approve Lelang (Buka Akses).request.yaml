$kind: http-request
url: "{{base_url}}/api/auctions/{{demo_auction_id}}/approve"
method: PUT
headers:
  Content-Type: application/json
auth:
  type: bearer
  credentials:
    token: "{{admin_token}}"
order: 2000
